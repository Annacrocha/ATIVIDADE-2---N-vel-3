#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <time.h>

int main() {
	setlocale(LC_ALL, "portuguese");
    srand(time(NULL));
    
    int random_number = (rand() % 100) + 1;
    
    printf("Número aleatório entre 1 e 100: %d\n", random_number);
    
    return 0;
}
